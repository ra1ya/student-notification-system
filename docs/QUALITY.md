# Quality Verification

This project uses GitHub Actions to verify formatting, static analysis, widget tests, an Android debug build, and PHP syntax on every push and pull request to `main`.

The workflow intentionally tracks the current Flutter stable channel so the portfolio project is continuously checked against a maintained Flutter toolchain.
